# LitePT 森林点云语义分割 — 使用说明

## 环境

```bash
conda activate litept
export PYTHONPATH=./
```

## 配置继承

```
configs/forest/
├── semseg-litept-small-v1m1.py        # 基础配置
├── semseg-litept-small-v1m1-local.py  # 本地覆盖（RTX 4080 16GB）
└── semseg-litept-small-v1m1-server.py  # 服务器覆盖
```

## 常改配置

| 参数 | 位置 | 说明 |
|---|---|---|
| `crop_type` | base | `"CylinderCrop"` 或 `"SphereCrop"` |
| `crop_point_max` | local/server | 采样点数（本地200000，服务器500000） |
| `grid_size` | base | 体素大小，默认 0.02m |
| `class_mapping` | base | 类别映射，默认 `{7: -1}` |
| `weight` | base 或 `--options` | 预训练权重路径 |
| `resume` | base 或 `--options` | 是否从 model_last.pth 恢复 |
| `batch_size` | local/server | 本地2，服务器4 |
| `data_root` | local/server | 数据路径 |
| `save_path` | local/server | 实验保存路径 |

### 裁切方式

- **CylinderCrop**（默认）：按 XY 距离排序取最近 N 个点，Z 轴完整保留。适合森林
- **SphereCrop**：按 3D 距离排序取最近 N 个点。原始方式

切换方法：改 base config 顶部 `crop_type`，或命令行 `--options crop_type=SphereCrop`

## 训练

### 本地训练

```bash
PYTHONPATH=./ python tools/train.py \
  --config-file configs/forest/semseg-litept-small-v1m1-local.py \
  --num-gpus 1
```

### 服务器训练

```bash
PYTHONPATH=./ python tools/train.py \
  --config-file configs/forest/semseg-litept-small-v1m1-server.py \
  --num-gpus 1
```

### 加载预训练权重

```bash
PYTHONPATH=./ python tools/train.py \
  --config-file configs/forest/semseg-litept-small-v1m1-local.py \
  --num-gpus 1 \
  --options weight=exp/forest/semseg-litept-small-v1m1/model/model_best.pth
```

### 恢复训练（断点续训）

```bash
PYTHONPATH=./ python tools/train.py \
  --config-file configs/forest/semseg-litept-small-v1m1-local.py \
  --num-gpus 1 \
  --options resume=True
```

### 命令行覆盖参数

```bash
# 覆盖 epoch 和 eval_epoch
--options epoch=100 eval_epoch=10

# 覆盖裁切方式和点数
--options crop_type=SphereCrop crop_point_max=102400
```

## 推理（大场景分块推理）

使用 `tools/batched_inference.py`，对大点云做 XY 滑窗分块推理：

### 本地推理

```bash
PYTHONPATH=./ python tools/batched_inference.py \
  --config-file configs/forest/semseg-litept-small-v1m1-local.py \
  --num-gpus 1 \
  --options weight=exp/forest/semseg-litept-small-v1m1/model/model_best.pth \
    input_file=data/tree/hainan/test_all/a/geomap_20260518_102832.pcd
```

### 服务器推理

```bash
PYTHONPATH=./ python tools/batched_inference.py \
  --config-file configs/forest/semseg-litept-small-v1m1-server.py \
  --num-gpus 4 \
  --options weight=exp/forest/semseg-litept-small-v1m1/model/model_best.pth \
    input_file=/data/dataset/hainan/test_all/a/geomap.pcd
```

### 推理参数

- `weight`: 模型权重路径（必须）
- `input_file`: 输入 PCD/LAS 文件路径（必须）
- `data.block_xy`: 分块大小（米），本地20，服务器40
- `data.overlap`: 块间重叠（米），本地5，服务器10

输出保存在 `input_file` 同目录，生成 `.pcd` 和 `.las` 可视化文件。

## 数据集

```
data/forest/          # → /mnt/data_disk/dataset/tree/forest （符号链接）
data/tree/            # → /mnt/data_disk/dataset/tree/ （推理用）
```

类别（7类）：
| ID | 类别 | 说明 |
|---|---|---|
| 0 | terrain | 地面 |
| 1 | foliage | 树叶 |
| 2 | CWD | 粗木质残体（极稀有） |
| 3 | trunk | 树干 |
| 4 | branch | 树枝 |
| 5 | snag | 枯立木 |
| 6 | non-tree-cyl | 非树圆柱体 |
| 7 | → -1 | 噪声（忽略） |