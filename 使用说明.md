# LitePT 森林点云语义分割 — 使用说明

## 目录
1. [环境配置](#1-环境配置)
2. [数据准备](#2-数据准备)
3. [训练模型](#3-训练模型)
4. [模型推理](#4-模型推理)
5. [预测结果可视化](#5-预测结果可视化)
6. [参数说明](#6-参数说明)

---

## 1. 环境配置

### 激活环境
```bash
conda activate litept
cd ~/workshop/deep_learning/LitePT
export PYTHONPATH=./
```

### 依赖库
| 库 | 版本 | 说明 |
|----|------|------|
| PyTorch | 2.4.1+cu121 | CUDA 12.1 |
| spconv | cu121 | 稀疏卷积 |
| pointrope | sm_89+sm_90 | 自定义 CUDA 算子（PointROPE） |
| pointops | sm_89+sm_90 | 自定义 CUDA 算子（点云操作） |
| laspy | - | LAS 文件读写 |

### GPU 信息
- 当前部署机器: RTX 4080 (compute 8.9, sm_89)
- CUDA: 12.1, PyTorch cu121

---

## 2. 数据准备

### 目录结构
```
LitePT/
├── data/forest/
│   ├── train/          # 训练 .las 文件
│   └── val/            # 验证 .las 文件
```

### LAS 文件格式
- 版本: LAS 1.4 (format 6)
- 坐标: XYZ (int32, scale=0.001 → 真实米制坐标)
- 标签字段: 自定义 `label` 字段 (float64, 0-6)

### 类别说明
| 标签 | 名称 | 说明 |
|------|------|------|
| 0 | terrain | 地面/地形 |
| 1 | foliage | 植被树叶 |
| 2 | CWD | 粗木质残体（倒木） |
| 3 | trunk | 树干主干 |
| 4 | snag | 断树/枯立木 |
| 5 | non-tree | 非树（预留类，无标注） |
| 6 | branch | 树枝（粗枝，细枝标为 foliage） |

---

## 3. 训练模型

### 3.1 训练命令

**使用预训练权重继续训练（推荐）:**
```bash
PYTHONPATH=./ python tools/train.py \
  --config-file configs/forest/semseg-litept-small-v1m1.py \
  --num-gpus 1 \
  --options epoch=100 eval_epoch=100 \
    weight=exp/forest/semseg-litept-small-v1m1-nuscenes/model/model_best.pth \
    save_path=exp/forest/semseg-litept-small-v1m1
```

**从头训练（无预训练权重）:**
```bash
PYTHONPATH=./ python tools/train.py \
  --config-file configs/forest/semseg-litept-small-v1m1.py \
  --num-gpus 1 \
  --options epoch=100 eval_epoch=100 save_path=exp/forest/<实验名>
```

**断点续训（从 model_last.pth 继续）:**
```bash
PYTHONPATH=./ python tools/train.py \
  --config-file configs/forest/semseg-litept-small-v1m1.py \
  --num-gpus 1 \
  --options resume=True weight=exp/forest/<实验名>/model/model_last.pth save_path=exp/forest/<实验名>
```

### 3.2 参数说明

| 参数 | 说明 | 示例值 |
|------|------|--------|
| `--config-file` | 配置文件路径 | `configs/forest/semseg-litept-small-v1m1.py` |
| `--num-gpus` | GPU 数量 | `1` |
| `epoch` | 训练总 epoch 数 | `100` |
| `eval_epoch` | 验证间隔（需整除 epoch） | `100` |
| `weight` | 预训练权重路径（可选） | `exp/forest/.../model_best.pth` |
| `save_path` | 实验输出目录 | `exp/forest/<实验名>` |
| `resume` | 是否从断点继续 | `True` |

### 3.3 训练输出
训练结果保存在 `exp/forest/<实验名>/` 下：
```
exp/forest/<实验名>/
├── model/
│   ├── model_best.pth    # 最佳验证 mIoU 的权重（用于推理）
│   ├── model_last.pth    # 最后一个 epoch 的权重
│   └── epoch_N.pth       # 每 3 epoch 保存的 checkpoint
├── train.log              # 训练日志
└── wandb/                 # wandb 日志（如启用）
```

---

## 4. 模型推理

### 4.1 推理命令

```bash
PYTHONPATH=./ python tools/test.py \
  --config-file configs/forest/semseg-litept-small-v1m1.py \
  --num-gpus 1 \
  --options save_path=exp/forest/<实验名> weight=exp/forest/<实验名>/model/model_best.pth
```

### 4.2 参数说明

| 参数 | 说明 |
|------|------|
| `--config-file` | 模型配置文件路径 |
| `--num-gpus` | GPU 数量（目前仅支持 1） |
| `save_path` | 输出目录，预测结果和日志存在这里 |
| `weight` | 模型权重文件路径 |

### 4.3 示例

**推理验证集:**
```bash
PYTHONPATH=./ python tools/test.py \
  --config-file configs/forest/semseg-litept-small-v1m1.py \
  --num-gpus 1 \
  --options save_path=exp/forest/semseg-litept-small-v1m1 weight=exp/forest/semseg-litept-small-v1m1/model/model_best.pth
```

### 4.4 推理输出

```
exp/forest/<实验名>/
├── result/
│   ├── <场景1>_pred.npy    # 每个场景的预测结果（每个点的类别 0-6）
│   └── <场景2>_pred.npy
└── test.log                 # 推理日志
```

`.npy` 文件是 numpy 数组，形状为 `(N,)`, N 为点的数量，每个值是对应点的预测类别。

---

## 5. 预测结果可视化

推理产生的 `.npy` 不能直接用 CloudCompare 打开。需要转换回 LAS 格式。

### 5.1 转换命令

使用项目自带的转换脚本 `tools/pred2las.py`：

```bash
PYTHONPATH=./ python tools/pred2las.py
```

默认转换 `data/forest/val/` 下所有已有 `_pred.npy` 的场景。

### 5.2 转换脚本用法（可自定义）

```python
# tools/pred2las.py 中修改 files 列表即可指定转换哪些场景
files = [
    ("场景名.las", "场景名.las_pred.npy"),
    ...
]
```

### 5.3 在 CloudCompare 中查看

1. 用 CloudCompare 打开转换后的 LAS 文件（`result/*_pred.las`）
2. 按 `Shift+A` 或 `Edit → Properties → Show label field`
3. CloudCompare 默认显示 `classification` 字段（原始值，全为 0）
4. **切换到 `label` 字段查看预测结果**: `Edit → Metadata → Change current field → label`
5. 按类别着色：`Classification → Scalar Field → Edit → SF color ramp`

### 5.4 对比 GT 与预测

同时打开两个文件：
- 原始 LAS（`data/forest/val/*.las`）— `label` 字段是 GT
- 预测 LAS（`result/*_pred.las`）— `label` 字段是预测结果

通过 `Tools → Labels/Predictions → Compare` 或分别设置不同颜色进行目视对比。

---

## 6. 参数说明

### 6.1 训练相关配置（configs/）

| 参数 | 说明 | 推荐值 |
|------|------|--------|
| `num_classes` | 类别数 | 7 |
| `in_channels` | 输入特征维度 | 3（仅 XYZ 坐标） |
| `batch_size` | 每 GPU batch size，**总 batch = batch_size × GPU数** | 4 |
| `epoch` | 训练总 epoch 数 | 600 / 100 |
| `eval_epoch` | 验证/保存间隔（需整除 epoch） | 60 / 100 |
| `num_worker` | DataLoader worker 数 | 4 |
| `enable_wandb` | 是否启用 wandb 日志 | `False` |

### 6.2 推理相关配置

| 参数 | 说明 | 推荐值 |
|------|------|--------|
| `save_path` | 输出目录 | 指向实验目录 |
| `weight` | 权重路径 | `model_best.pth` |

### 6.3 数据集配置

| 参数 | 说明 |
|------|------|
| `data.train.type` | 固定为 `ForestDataset` |
| `data.val.type` | 固定为 `ForestDataset` |
| `data.test.type` | 固定为 `ForestDataset` |
| `data.train.split` | 数据划分，`train` 或 `val` |
| `data.train.data_root` | LAS 文件所在目录 |
| `data.num_classes` | 类别数 |
| `data.names` | 类别名称列表 |
| `data.ignore_index` | 忽略的类别（通常为 -1） |

---

## 常见问题

### Q: 训练时 GPU 显存不足
- 减小 `batch_size`（原始 4）
- 减小模型大小（如用 tiny 版本）

### Q: 推理很慢
- 使用默认配置即可
- 每个场景约 1-2 分钟

### Q: CloudCompare 打开 LAS 显示"未分类"
- 原始 LAS 的 `classification` 字段为 0（未使用）
- 标签存在自定义 `label` 字段
- 在 CloudCompare 中切换到 `label` 字段显示

### Q: 如何添加新的数据
- 将 LAS 文件放入 `data/forest/train/` 或 `data/forest/val/`
- 确保文件扩展名为 `.las`
- 确保包含 `label` 自定义字段（float64, 0-6）

### Q: 如何使用预训练权重
- 预训练权重路径: `exp/forest/semseg-litept-small-v1m1-nuscenes/model/model_best.pth`
- 在训练命令中通过 `weight=` 参数指定